# DisCo DETR — Python dependencies
# Tested with Python 3.8, PyTorch 1.8.1, CUDA 11.7 (Conditional / Deformable DETR).
# Install a torch build matching your CUDA *before* the rest, e.g.:
#   pip install torch==1.8.1 torchvision==0.9.1

matplotlib
mmcv==1.0.3
numpy
Pillow<=6.2.2          # pinned for compatibility with the pinned torchvision
packaging
six
terminaltables
scikit-learn
faiss-gpu==1.6.1
tensorboard
future
tqdm
ipdb
git+https://github.com/cocodataset/cocoapi.git#subdirectory=PythonAPI&egg=pycocotools

# Deformable attention: compile the CUDA operator following the Deformable DETR repo.

# --- RT-DETR variant (optional) ---
# The RT-DETR pre-training variant requires a newer stack:
#   torch>=2.0.0   (e.g. torch==2.0.1, torchvision==0.15.2)
#   onnx==1.14.0
#   onnxruntime==1.15.1
#   PyYAML
#   scipy
